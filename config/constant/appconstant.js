/**
 * Application Constants.
 */

var define = require("node-constants")(exports);

define({
    /* DAYS_IN_WEEK: 7,
    SECONDS_IN_MINUTE: 60 */
	
	ROOT_DIR: __dirname,
	APP_PORT: 4935

	/*
	 * To obtain a constant use: console.log('App Constant: ' + AppConstant.APP_PORT);
	 * 
	 * 
	 */
	
	
});